server {
  listen 80 default;

  location / {
    resolver 127.0.0.11 valid=30s;
    proxy_pass http://server:3000/;
  }

  location /assets/ {
    resolver 127.0.0.11 valid=30s;
    proxy_pass http://frontend:80/;
  }
}
